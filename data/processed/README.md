# Processed Data

Store generated preprocessing outputs here, such as resized image pairs, masks, chips, cached metadata, or train/validation splits.

This folder is ignored by Git except for this README.
