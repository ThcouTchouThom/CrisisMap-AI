# Raw Data

Extract the official xBD/xView2 archive into this folder, preferably as:

```text
data/raw/xbd/
```

This directory is ignored by Git because the dataset is large. Do not add raw satellite imagery or full label archives to version control.
