# Local xBD Archives

Place the externally shared xBD/xView2 archive files here before running setup:

- `train_images_labels_targets.tar`
- `xview_geotransforms.json.tgz`

These archive files are intentionally ignored by Git.
